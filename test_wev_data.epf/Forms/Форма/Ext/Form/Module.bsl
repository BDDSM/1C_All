

&НаКлиенте
Процедура Команда1(Команда)
#Если  ВебКлиент Тогда 
	 //СтрокаВремяОжиданияОпаздывающегоПассажира = Год(0) + Месяц(0) + Число(0) + Час (10) + Минута(45) + Секунда(25);
	 //ВремяОжиданияОпаздывающегоПассажира  = Дата(ТекущаяДата()) //СтрокаВремяОжиданияОпаздывающегоПассажира);  'ГГГГММДДччммсс', где:
	 ОчиститьСообщения();
Сообщить(Дата(СтрокаДаты));
Сообщить("веб")
#Иначе 
	
	Сообщить(Дата(СтрокаДаты)) 
#КонецЕсли
	
	
КонецПроцедуры

