
&НаКлиенте
Процедура Преобразовать(Команда)
	Часов = Час(ДатаДляПреобразования);
	Минуты = Минута(ДатаДляПреобразования);
	РезультатПреобразования = ((Часов*60*60) + (Минуты*60))/3600;
КонецПроцедуры
